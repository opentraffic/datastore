package io.opentraffic.datastore;

/**
 * Type of vehicle from which the measurement was taken.
 *
 * At the moment, only AUTO (automobile, car) is supported,
 * although other types may be supported in the future.
 */
public enum VehicleType {
    AUTO;
}
